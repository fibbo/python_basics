number_list = list(range(100))

for number in number_list:
    if number % 2 == 0:
        print(f"{number} is even.")

guest_list = ["Tom", "Anna", "Robert"]

for guest in guest_list:
    print(f"{guest} is coming to the party")
