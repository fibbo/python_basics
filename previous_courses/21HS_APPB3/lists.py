# n_elements = int(input("How many elements should be in the list? "))

# number_list = list(range(n_elements))

# print(number_list)

n_start = int(input("Please enter the first number: "))
n_end = int(input("Please enter the last number (not included) "))

number_list = list(range(n_start, n_end))

print(number_list)
